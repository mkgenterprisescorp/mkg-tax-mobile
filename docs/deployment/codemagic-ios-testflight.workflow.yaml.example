# HISTORICAL EXAMPLE — the live `ios_testflight` workflow now lives in root
# `codemagic.yaml` (promoted after explicit owner approval + prepare validation
# on main @ 74cf97f / build 6a625310).
#
# Keep submit_to_app_store: false. Bundle ID must remain
# com.mkgenterprises.mkgTaxMobile. Integration label: Codemagic CI.
#
# See root codemagic.yaml → workflows.ios_testflight for the active definition.
